# Audtheia Species Detection Proxy — Dependencies
# Pinned for reproducible Render.com deployments.
# Update with care and re-test after any version change.

fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.20
httpx==0.28.1
slowapi==0.1.9
